using System;

namespace ScopesExercise
{
    class Program
    {
        static int w = 1000;
        static void Main(string[] args)
        {
            do {

                cleanYourRoom();

                // do stuff

            } while(isYourRoomDirty());
                // check if need to do loop again after do is done
                // check, after you do the loop
           







            // int sum = 0;
            // for(int i=1; i <= 100; i++)
            // {
            //     sum = sum +i;
            // }
            // Console.WriteLine("sum of 1 to 100 is "+sum);

            // int sum2 = 0;
            // int i2 = 1;
            // while(i2 <= 100)
            // {
            //     sum2 = sum2 + i2;
            //     i2++;
            // }
            // Console.WriteLine("sum of 1 to 100 is "+sum2);



             // String[] students = new String[]{"Johna","Janis","Matthias"};

            // for(int i = 0; i < students.Length; i++)
            // {
            //     Console.WriteLine(i+" => "+ students[i]);          

            // }

            // for loop above says essentially the same as foreach loop below

            // int index = 0;
            // foreach(String student in students){
            //     Console.WriteLine(student);
            //     index++;
            // }  
        }

        // define a method called sum
        // takes in as input two integers
        // prints out the two integers
        // and returns their sum

        // static int Sum(int x, int y){
        //     w = w*2;
        //     int sum = x+y;
        //     Console.WriteLine("In Sum() w = "+w);
        //     Console.WriteLine("inside sum(), x = "+x);
        //     Console.WriteLine("inside sum(), y = "+y);
        //     return sum;


        


        // }
        // static Product(){
        //     int product = product(a * b);


        // }
    }
}
